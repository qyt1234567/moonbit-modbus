# MoonBit 工业 Modbus 协议栈｜项目申报书

**项目名称**：MoonBit 工业 Modbus 协议栈（moonbit-modbus）  
**参赛者**：qyt1234567  
**项目仓库**：https://github.com/qyt1234567/moonbit-modbus  
**Gitlink 仓库**：https://gitlink.org.cn/qytqyt/moonbit-modbus

## 项目概述

本项目面向 PLC、变频器、工业仪表和边缘网关，使用 MoonBit 实现可复用的 Modbus 协议核心。项目统一处理 Modbus RTU、ASCII、TCP 三种传输形态，提供 PDU/ADU 编解码、CRC16、LRC、MBAP、异常响应、增量解析和寄存器类型映射，降低 MoonBit 工业设备接入的门槛。

## 目标与价值

当前 MoonBit 生态中尚未发现成熟的 Modbus 协议栈。本项目不绑定具体操作系统或串口实现，先提供稳定、可测试、可移植的协议边界，后续可复用于设备驱动、数据采集、网关和仿真测试。与通用 buffer/socket 库相比，本项目聚焦工业协议语义、帧边界和校验正确性。

## 实施范围

第一阶段完成 RTU、ASCII、TCP 编解码，CRC16/LRC，MBAP 解析，增量解析器，常用功能码和寄存器映射。第二阶段扩展 Client/Server、虚拟传输层、功能码 01/02/04/05/0F/10、设备寄存器模板与轮询器。第三阶段增加网关桥接、性能基准和 OPC UA 等上层适配。

## 交付物与质量保证

交付 MoonBit 源码、API 文档、可运行示例、协议测试、来源说明和 Apache-2.0 License；GitHub 与 Gitlink 保持同步。项目配置 MoonBit 0.10.3 CI，执行 `moon fmt --check`、`moon check --deny-warn`、`moon test --target all --deny-warn` 与 `moon info`。源码、测试和示例独立编写，协议字段依据公开 Modbus 规范重新实现，无第三方代码复制。
